<fieldset>
  <%= f.label 'Minor(s)' %> 
  <%= f.collection_select(:minor_ids, Minor.all, :id, :name, {:prompt => 'Choose a minor...'}, 
								   {:name => 'user[minor_ids][]', 
								    class: 'dynamic-select',
								    :multiple => true }) %>
</fieldset>
