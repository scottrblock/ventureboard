<%= form_for(@minor) do |f| %>
  <% if @minor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@minor.errors.count, "error") %> prohibited this minor from being saved:</h2>

      <ul>
      <% @minor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
